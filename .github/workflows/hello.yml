---
name: "Dogu's first CI file"
on:
  push:
    branches:
      - master

jobs:
  hello_world_job:
    runs-on: ubuntu-18.04
    steps:
      - name: "Say hello"
        run: | 
          echo "pinarbasi!"
      - name: "Now it is"
        run: |
          date
      - name: "Directory content"
        run: |
          ls -al
...